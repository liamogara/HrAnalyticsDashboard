# HrAnalyticsDashboard
A small project to simulate a workforce analytics system used by HR. Uses a SQLite database, processes the data with SQL queries and a Python script, and organizes it into a simple Power BI dashboard.
